# Base database connection (required for all environments)
DATABASE_URL=postgresql://username:password@postgres.bogach.es:5432/dbname

# Optional: Override the complete connection URL with schema
# Recommended for production/develop branches
# If not set, will be computed as: ${DATABASE_URL}?schema=${schema_name}
# DATABASE_URL_WITH_SCHEMA=postgresql://username:password@postgres.bogach.es:5432/dbname?schema=public

# Alternative override: Set just the schema name (less common)
# DB_SCHEMA=public

# For local testing of preview behavior
# VERCEL_GIT_COMMIT_REF=feature/test-branch
# VERCEL_GIT_PULL_REQUEST_ID=123
